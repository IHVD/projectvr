using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SnapOnPoint : MonoBehaviour {
	public int pointType;
	public ExperimentController.ExperimentDangers danger;

	//maybe put "something collided with me, now snap to me bby" in here, so that the student itself doesnt have the trigger variable.
}
